To avoid directly having very similar configuration files, these ablations use two `generate.py` scripts to generate the individual configuration yml files for each ablation.
Run these and then run the resulting configurations as normal.
This should eventually be refactored into a shiny new multi-experiment configuration system.
